import React from 'react';

const FooterWave: React.FC = () => {
  // Basic yellow block, replace with SVG or CSS wave later if needed
  return (
    <div className="w-full h-24 md:h-32 bg-yellow-200 absolute bottom-0 left-0 right-0 z-0">
      {/* You could add an SVG wave shape here */}
    </div>
  );
};

export default FooterWave; 